# Future enhancements

Items intentionally deferred after Phase 0–4:

- **Natural-language custom chart builder** — full “show sales by region as a stacked bar” with LLM-picked axes (current: bar/line/pie/area + auto column detection).
- **AutoML** (AutoGluon / FLAML) for predictive models beyond Prophet z-score/forecast.
- **YAML ETL pipeline DSL** with plugin hooks.
- **OpenLineage** integration for enterprise lineage.
- **Snowflake / BigQuery** connectors.
- **Cloudflare / Docker-VPN** live tunnel implementations in ETL.
- **Playwright E2E** test suite.
- **Public REST API** for external credential submission.
- **GDPR PII masking** on extract and NLQ results.
